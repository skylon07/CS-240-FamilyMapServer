package services.requests;

/**
 * The generic class for all requests. Currently the only purpose this serves
 * is as a generic request datatype and a placeholder for possible changes that
 * need to be applied to all request types.
 * 
 * Note that all requests should be read-only (only have getters for properties)
 */
public abstract class GenericRequest {
    // this block intentionally left blank
}
